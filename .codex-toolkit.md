# Codex Toolkit Configuration

Project-specific settings for codex-toolkit commands.
Generated by `/init`. Edit freely — all fields are optional.

## Project

- **Stack**: Go, Hertz (HTTP), GORM (ORM), Uber FX (DI)
- **Test command**: `go test ./...`
- **Source directories**: `handler/, service/, dal/, model/, injection/, config/`

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.4
- **Default effort**: medium
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
vendor/
.git/
*.pb.go
resources/
dal/query/
conf/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is a Go blogging platform (Sonic) using Cloudwego Hertz as the HTTP framework,
GORM for database access, and Uber FX for dependency injection.

Key conventions:
- Context timeouts: 5s default, 10s+ for complex queries (see CONTEXT_TIMEOUT_GUIDE.md)
- No fx.Populate — all DI wiring goes through fx.Provide in injection/
- CSRF header (X-CSRF-Token) required for all state-changing endpoints
- Auth via JWT middleware in handler/middleware/
- AI provider abstraction lives in service/ai/; backends in service/ai/impl/
- GORM query code is generated in dal/query/ — do not hand-edit those files
- Passwords are bcrypt-hashed; validate all file paths from user input before file ops
- Supports SQLite3 (default), MySQL, PostgreSQL
```
